# Authorized recovery of the Lighthouse formal execution

This is an operational repair, authorized by the user's “授权” after the
request to repair failure handling, validate, and rerun only failed/missing
trajectories. The scientific extension protocol remains byte-for-byte unchanged.

## Incident and immutable evidence

Parent run: `20260908T003900Z_8cd8376_lighthouse_formal_v1`.
Parent commit: `8cd8376c56f983525be601bfbf5386736e4754c9`.

The initial presence census found 1,396 results, 201 undispatched rows, and
three interrupted dispatches. Presence alone is not acceptance: every retained
result must pass the existing strict attempt/selector replay and authorization
audits, regardless of success or failure outcome.

- Row 1447 (PatternLock/23/OC5) failed during simulator initialization with
  `AcceleratorError: CUDA error: out of memory`, before policy reset or actions.
  The original ledger classified this as `hard_stop`, `retry_allowed=false`.
  Cleanup then raised FileNotFoundError for the absent resident reset receipt.
  That original classification is NOT rewritten. The reviewed initialization
  OOM is explicitly authorized for recovery by this incident-specific record.
- Row 1352 (InsertPeg/26/OC3) was interrupted with evaluator signal 143 by the
  controller stop, with no complete scientific result.
- Row 1354 (InsertPeg/27/OC3) was dispatched but interrupted before evaluator
  startup. It has no scientific result or attempt manifest.

No ordinary negative result is eligible for replacement. Any unexpected
incomplete row, damaged completed artifact, active/unconfirmed child process,
changed source outside the repair allowlist, or changed scientific input is a
stop for review, not permission to broaden recovery.

## Repair and validation

1. Cleanup honestly records a missing reset as null for a proved pre-action
   failure; completed scientific results still require a real reset receipt.
2. Reconciliation status 80 is inspected against the ledger, not mistaken for
   a missing reconciliation result. Hard stops remain hard stops.
3. A proved infrastructure failure does not abort other resident queues.
   Failed rows are not implicitly retried. Full cleanup is audited after the
   resident session closes; per-row checks do not require its future exit file.
4. Run CPU regression tests and all original architecture and 48 validation
   smoke cases on the clean repair commit before formal recovery.

## Recovery provenance and retry budget

Prepare a NEW run root using the full original 1,600-row matrix and fresh smoke
evidence. Bind a write-once recovery plan containing the parent's content
inventory, strict audit, retained row IDs, and the exact missing complement.
The plan and each submission bind one another by SHA-256. Never copy, edit,
relabel, or overwrite the parent's artifacts or manifests.

The new root has local attempt numbering starting at zero. A separate explicit
global retry offset is one for each of the three interrupted parent dispatches
and zero for previously undispatched rows. The global attempt index must never
exceed two (initial plus at most two infrastructure retries). This offset is
checked at submission and aggregation; creating a new root does not reset the
retry allowance. Further retries still require exact preceding failure evidence
and explicit selection; unknown hard stops require review.

Final aggregation re-audits the sealed parent and new root together. It requires
exactly one first-valid result for all 1,600 scientific keys, all 800 paired
initial-condition and non-treatment invariant checks, and every failed attempt's
evidence. Statistical calculations use the UNCHANGED frozen analysis and report
builder. The report discloses both commits, roots, per-row source, incident
override, and GPU/latency comparability limitations; it must not claim a
single-commit run. No partial or available-case final aggregate is permitted.

GPU allocation may change without changing the experiment: select currently
ample-memory shared A100 devices, retain models across rows, record resource
snapshots, do not interfere with other users, and do not alter model precision,
seeds, action horizon, timeout, selector, tasks, or weights. Preparation and
auditing are CPU work; only validated smoke and missing formal trajectories use
GPU execution. No GitHub push is authorized by this recovery approval.
